<% if instrument.user == current_user %>
	<!-- Implement Pundit to avoid unauthorized changes via URL -->
	<%= link_to 'Edit', edit_instrument_path(instrument) %>
	<%=
		link_to 'Delete',
		instrument_path(instrument),
		method: :delete,
		data: { confirm: 'Sure?' }
	%>
<% end %>
