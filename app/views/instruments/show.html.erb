<h1>
  <%= @instrument.category %> |
  <small><%= link_to 'Edit', edit_instrument_path(@instrument) %></small>
  <small>
    <%= link_to 'Destroy', instrument_path(@instrument),
                           method: :delete,
                           data: { confirm: 'Sure?' } %>
  </small>
</h1>
<h4><strong>Owner: </strong><%= @instrument.member.email %></h4>
<p><small><strong>Address: </strong><%= @instrument.address %></small></p>
<p><%= @instrument.description %></p>

